"""Authorization layer: the JSON-backed ``allowlist``, the aiogram middleware
(``access``), the code-mode ``permissions`` gate, and the per-option settings
access model (``settings_schema``) (#302)."""
