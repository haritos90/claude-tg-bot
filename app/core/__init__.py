"""Session runtime: the Claude Agent-SDK engine, session lifecycle + worker,
OAuth token refresh, and the schedule runner (#302). ``agent_context.md`` (the
agent self-description loaded by ``engine``) lives here as a co-located asset."""
