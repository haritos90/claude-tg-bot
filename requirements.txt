# Runtime dependencies — Python 3.11+
# Setup:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#
# NOTE: claude-agent-sdk drives the Claude Code CLI (`claude`), which must be
# installed and logged in to your Claude Pro/Max subscription. See README.md
# (no ANTHROPIC_API_KEY is used — everything runs on the subscription).

aiogram==3.28.2
claude-agent-sdk==0.2.101
aiosqlite==0.22.1
python-dotenv==1.2.2
# Pillow: render wide tables as PNG images (#162 — Telegram has no native table and a
# wide monospace grid wraps; narrow tables stay text grids). Uses the system
# DejaVuSansMono font (Cyrillic-safe). See table_image.py.
Pillow==12.2.0
# cairosvg: rasterize a chat reply's inline <svg> diagram to PNG so it can be sent as an
# image (#295 — Claude has no image generator but produces clean vector SVG; Telegram can't
# render SVG inline). Needs the system library libcairo2 (apt install libcairo2). See
# svg_image.py. If rendering fails the bot falls back to sending the raw .svg document.
cairosvg==2.9.0
