# Optional dependency for the local voice-note transcription feature (#363).
#
#   pip install -r requirements-voice.txt
#
# ALSO requires the ffmpeg SYSTEM binary on PATH (Debian/Ubuntu: `apt-get install ffmpeg`)
# to decode Telegram Opus/OGG voice notes to 16 kHz mono PCM.
#
# Runs fully on-device (CTranslate2, CPU int8) — no audio leaves the host, no API key.
# Without these installed, VOICE_TRANSCRIPTION stays effectively off: the bot detects the
# missing pieces and replies that voice transcription is unavailable. See docs/voice.md.
faster-whisper>=1.0
